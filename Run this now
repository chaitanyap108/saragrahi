# stop anything old
pkill -f "tinacms" 2>/dev/null || true
pkill -f "next dev" 2>/dev/null || true
fuser -k 4001/tcp 2>/dev/null || true
fuser -k 3000/tcp 2>/dev/null || true

# clear stale generated client only (safe)
rm -rf tina/__generated__ tina/tina-lock.json

# start local stack
npm run dev
